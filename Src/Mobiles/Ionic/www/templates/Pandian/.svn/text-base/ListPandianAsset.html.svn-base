
<ion-view view-title="进行盘点">
    <ion-nav-buttons side="right">
        <a class="button button-icon icon ion-plus" ng-click="onToggleMenu()"></a>
    </ion-nav-buttons>
    <ion-content overflow-scroll="false">
        <div class="item row">
            <div class="pd10">
                <span>盘点单：</span>{{PandianInfo.Named}}
            </div>

        </div>
        <div class="item row">
            <div class="pd10 row">
                <div class="col-50">创建人：{{PandianInfo.UserName}}</div>
                <div class="col-50">资产总数：{{PandianInfo.TotalQty}}</div>
            </div>
        </div>
        <div class="padding">
            <ul class="slide-tab">
                <li ng-class="slideIndex == 0 ? 'current':''"><a ng-click="activeSlide(0)">待盘点<br /> <i class="badge badge-positive">{{ListNotPandian.length}}</i></a></li>
                <li ng-class="slideIndex == 1 ? 'current':''"><a ng-click="activeSlide(1)">已盘点<br /> <i class="badge badge-positive">{{ListPandian.length}}</i></a></li>
                <li ng-class="slideIndex == 2 ? 'current':''"><a ng-click="activeSlide(2)">盘盈<br /> <i class="badge badge-positive">{{ListPanying.length}}</i></a></li>
            </ul>
        </div>

        <div class="pdlr10" ng-show="slideIndex == 0">
            <div class="dg-list" ng-hide="!ListNotPandian || ListNotPandian.length==0">
                <div class="box" ng-repeat="item in ListNotPandian">
                    <div class="row">
                        <div class="col col-80" ng-click="toggleGroup(item)" ng-class="{active: isGroupShown(item)}">{{$index + 1}}-资产名称：{{item.AssetName}}</div>
                        <div class="col col-20" ng-click="onPandianAsset(item)">
                            <span class="fr badge badge-positive">{{item.StatusName}}</span>
                        </div>
                    </div>
                    <div class="subbox" ng-show="isGroupShown(item)">
                        <div class="row">
                            <div class="col">条形码：{{item.Barcode}}</div>
                            <div class="col">计量单位：{{item.Unit}}</div>
                        </div>
                        <div class="row">
                            <div class="col">规格型号：{{item.SpecModel}}</div>
                        </div>
                        <div class="row">
                            <div class="col">使用部门：{{item.UseDepmtName}}</div>
                        </div>
                        <div class="row">
                            <div class="col">管理部门：{{item.MgrDepmtName}}</div>
                        </div>
                        <div class="row">
                            <div class="col">存放地点：{{item.StoragePlaceName}}</div>
                        </div>
                        <div class="row">
                            <div class="col">使用人：{{item.UsePersonName}}</div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="pdlr10" ng-show="slideIndex == 1">
            <div class="dg-list" ng-hide="!ListPandian || ListPandian.length==0">
                <div class="box" ng-repeat="item in ListPandian">
                    <div class="row">
                        <div class="col col-80" ng-click="toggleGroup(item)" ng-class="{active: isGroupShown(item)}">{{$index + 1}}-资产名称：{{item.AssetName}}</div>
                        <div class="col col-20" ng-click="onPandianAsset(item)">
                            <span class="fr badge badge-positive">{{item.StatusName}}</span>
                        </div>
                    </div>
                    <div class="subbox" ng-show="isGroupShown(item)">
                        <div class="row">
                            <div class="col">条形码：{{item.Barcode}}</div>
                            <div class="col">计量单位：{{item.Unit}}</div>
                        </div>
                        <div class="row">
                            <div class="col">规格型号：{{item.SpecModel}}</div>
                        </div>
                        <div class="row">
                            <div class="col">使用部门：{{item.UseDepmtName}}</div>
                        </div>
                        <div class="row">
                            <div class="col">管理部门：{{item.MgrDepmtName}}</div>
                        </div>
                        <div class="row">
                            <div class="col">存放地点：{{item.StoragePlaceName}}</div>
                        </div>
                        <div class="row">
                            <div class="col">使用人：{{item.UsePersonName}}</div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="pdlr10" ng-show="slideIndex == 2">
            <div class="dg-list" ng-hide="!ListPanying || ListPanying.length==0">
                <div class="box" ng-repeat="item in ListPanying">
                    <div class="row">
                        <div class="col col-80">{{$index + 1}}-{{item.Barcode}}</div>
                        <div class="col col-20" ng-click="onPandianAsset(item)">
                            <span class="fr badge badge-positive">{{item.StatusName}}</span>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        
    </ion-content>
</ion-view>